services:
  strava_kudos_local:
    image: strava_kudos_local
    build: .
    container_name: strava_kudos_local
    restart: on-failure:2
    volumes:
      - ./config.json:/app/config.json:ro
      - ./.env:/app/.env:ro
      - ./athleteState.json:/app/athleteState.json
